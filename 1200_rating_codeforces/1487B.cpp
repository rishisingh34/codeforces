#include<bits/stdc++.h>
using namespace std ;

int main() {
    int test ;
    cin >> test ;
    while(test--) {
        int n, k  ; 
        cin >> n >> k ; 
        if(n%2 == 0) {
            cout << (k%n) + 1  << endl ; 
            continue; 
        }

        
        
    }
}